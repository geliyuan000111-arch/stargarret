# 星星阁楼 使用手册

## 设置页

点击左上角头像图标进入设置页，可以：
- **关联 API Key**：粘贴你的 Gemini API Key，保存后 AI 功能立即生效
- **导出备份**：把所有笔记导出为 JSON 文件，防止数据丢失
- **从备份恢复**：选择之前导出的 JSON 文件恢复数据（会替换当前所有记录，操作前需确认）
- **清空所有数据**：需要点击两次确认才会执行
- **闲聊叽人设**：自定义昵称、人设设定、对你的理解（每项上限 100 字），保存后闲聊叽将按你设定的人设回复。不填则使用默认小叽人设，**不影响工作叽**。

> 如果没有填写 API Key，左下角会有引导提示，点击 × 可以关闭。

---

## 两种模式

右上角可以切换模式：

| 模式 | 用途 |
|------|------|
| **工作叽** | 记笔记、存想法、管待办 |
| **闲聊叽** | 和小叽聊天、倾诉、解压 |

两个模式的内容完全隔离，互不干扰。

---

## 工作叽：记笔记

### 直接输入
随手输入任何内容，AI 会自动判断分类：

```
今天想做一个读书打卡的小工具
→ 自动归入「想法」

明天下午三点开项目会
→ 自动归入「待办」

https://example.com/article
→ 自动归入「链接」，并解析标题和摘要
```

### 用指令强制分类
格式：`/分类名 内容`

```
/学习 费曼学习法：用教别人的方式来检验自己是否真的理解
/工作 Q3 OKR 初稿发给 leader 确认
/aha-moment 原来专注力也是可以训练的肌肉
```

底部指令栏会自动展示你**使用频率最高的 5 个指令**，用得越多越靠前，点击可快速填入。

### 在分类里搜索
点击左侧任意分类进入详情页，输入框变成搜索框，支持 AI 语义搜索——不需要记住原文，描述意思就能找到。

---

## 工作叽：管理笔记

每条笔记支持：

- **打勾**：标记完成，内容显示删除线
- **优先级**：点击右上角「优先级」按钮，可标记「紧急」「本周」「下周」「暂停」，再次点击取消
- **备注**：直接在内容下方输入框补充说明
- **标签**：点击「+ 标签」添加自定义标签，可勾选「保存以便复用」下次快速选用，标签右侧 × 可删除
- **删除**：鼠标悬停后右下角出现删除按钮

---

## 闲聊叽：和小叽聊天

切换到闲聊叽后，直接输入就能和小叽对话。小叽是个温柔治愈的陪伴机器人，说话会在句尾加「叽」。

### 羁绊总结
输入 `/羁绊总结`，小叽会把最近的聊天内容总结成一段温馨的记录，自动保存到左侧「羁绊」分类。

---

## 彩蛋

- 输入包含 `呜呜` `想哭` `难过` `累了` `不开心` 的内容，小叽会抱抱你
- **3月30日**、**12月25日** 打开 app 会有特别问候

---

## 数据说明

所有笔记存在本地浏览器缓存（localStorage），不上传到任何服务器。清除浏览器缓存或重装 app 会丢失数据，重要内容注意备份。

---

## 常见问题

**AI 分类不准怎么办？**
用 `/分类名 内容` 的格式手动指定，优先级高于 AI 判断。

**链接没有解析出内容？**
部分网站有反爬限制，Gemini 搜索工具无法访问时会显示默认文案，不影响链接本身的保存。

**AI 功能完全不响应？**
检查网络是否能访问 Google 服务。API key 有调用频率限制，免费额度用完后需要等重置或升级。
